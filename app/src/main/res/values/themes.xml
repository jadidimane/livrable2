<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.DZLD" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>
    <style name="StyleTV">
        <item name="android:textSize">@dimen/livSizeTV</item>
        <item
            name="android:textColor">@color/livTextViewColor</item>
        <item name="android:layout_weight">1</item>
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">wrap_content</item>
    </style>
    <style name="StyleButton">
        <item name="android:textSize">@dimen/livSizeButton</item>
        <item
            name="android:textColor">@color/livButtonColor</item>
        <item name="android:layout_weight">1</item>
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">wrap_content</item>
    </style>
    <style name="Theme.DZLD" parent="Base.Theme.DZLD" />
</resources>